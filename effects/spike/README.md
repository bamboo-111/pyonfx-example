## Spike Effects

This folder contains two PyonFX subtitle effect scripts:

- `spike.py`: original spike melt version
- `spike_inverse.py`: inverse-entry variant with slower reverse dissolve and earlier spike generation
